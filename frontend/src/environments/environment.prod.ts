export const environment = {
  production: true,
  // Base URL of the Laravel backend (no trailing slash).
  backendUrl: 'https://api.vendora.app',
  // Base URL of the JSON API.
  apiUrl: 'https://api.vendora.app/api',
};
